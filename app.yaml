application: catfacts-io
version: 1
runtime: python27
api_version: 1
threadsafe: yes

handlers:
- url: /assets
  static_dir: static

- url: /subscribe.*
  script: controllers.subscriptions.app

- url: /.*
  script: main.app

libraries:
- name: webapp2
  version: "2.5.2"
